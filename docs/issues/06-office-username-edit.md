## 🏷️ 種類（あてはまるものに `x` を入れる）

- [ ] 🐛 バグ・不具合（動きがおかしい）
- [x] ✨ 新しい機能・改善のお願い
- [ ] ❓ 質問・相談
- [ ] 📄 ドキュメント（説明文）の修正

---

## 📝 タイトルで伝えたいこと（一言で）

表示名（ユーザーネーム）を `/office` から変更できるようにする

---

## 📖 くわしい内容

表示名は `user_metadata.name` に保存されますが、変更する UI がなく、サインアップ時のままです。
office 内のプロフィール小窓から手軽に変更でき、変更が即座に Presence（他ユーザーからの見え方）へ
反映されるようにします。

---

## 🔁 再現手順（バグのときだけ）

特になし（改善のため該当なし）

---

## 🎯 本来どうなってほしいか（期待する結果）

- office 内から表示名を変更できる
- `supabase.auth.updateUser()` で保存され、リロード後も保持される
- 変更が、自分と他ユーザーの office 表示に即時反映される
- 空欄・長すぎる名前などのバリデーションがある

## ⚠️ 実際にどうなったか（現状）

特になし（改善のため該当なし）

---

## 🖼️ 参考（スクリーンショット・リンクなど）

- 表示名の保存先: Supabase Auth `user_metadata.name`（`src/app/signup/page.tsx` で設定）
- 反映先: office Presence（`src/app/office/World.tsx`）ほか、名前を表示する箇所

---

## 💻 環境（バグのときだけ）

特になし

---

## 📌 補足・メモ

- ③アバターステータス・シャツ色と同じ「プロフィール小窓」にまとめると自然。
- 表示名を参照している他画面（tasks の担当表示など）への波及がないか確認する。
